<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ch.epfl.sweng.eventmanager">

    <application
        android:name=".EventManagerApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.eventSelector.EventPickingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.eventShowcase.MapActivity"
            android:parentActivityName=".ui.eventShowcase.EventActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.eventShowcase.EventActivity" />
        </activity>
        <activity
            android:name=".ui.eventShowcase.TicketActivity"
            android:parentActivityName=".ui.eventShowcase.EventActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.eventShowcase.EventActivity" />
        </activity>
        <activity
            android:name=".ui.schedule.ScheduleActivity"
            android:parentActivityName=".ui.eventShowcase.EventActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.eventShowcase.EventActivity" />
        </activity>
        <activity
            android:name=".ui.userManager.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".ui.userManager.DisplayAccountActivity"
            android:label="@string/title_activity_account_display" />
        <activity android:name=".ui.eventShowcase.EventShowcaseActivity"></activity>
    </application>

</manifest>