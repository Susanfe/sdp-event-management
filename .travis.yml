language: android
sudo: required # gives us actual VMs that are longer to boot but faster to build
env:
- API_VERSION=16
- API_VERSION=15
- API_VERSION=17
- API_VERSION=18
- API_VERSION=24
matrix:
  allow_failures:
  - env: API_VERSION=15
  - env: API_VERSION=17
  - env: API_VERSION=18
  - env: API_VERSION=24
android:
  components:
    # The BuildTools version used by your project
    - build-tools-27.0.3
    # The SDK version used to compile your project
    - android-28
    # The SDK version used by the system image
    - android-$API_VERSION
    # The system image, to run an emulator during the tests
    - sys-img-armeabi-v7a-android-$API_VERSION
before_script:
  # Emulator Management: Create, Start and Wait
  - echo no | android create avd --force -n test -t android-$API_VERSION --abi armeabi-v7a --skin 768x1280
  - emulator -avd test -no-audio -no-window -no-boot-anim -camera-back none -camera-front none -verbose &
  - android-wait-for-emulator
  - adb shell input keyevent 82
  # Disable transitions
  - adb shell settings put global window_animation_scale 0.0
  - adb shell settings put global transition_animation_scale 0.0
  - adb shell settings put global animator_duration_scale 0.0
  # Set twitter key
  - sed -i 's/${CONSUMER_SECRET}/$TWITTER_CONSUMMER_SECRET/' app/src/main/res/values/twitter.xml
    # Set up Code Climate test reporter
  - curl -L https://codeclimate.com/downloads/test-reporter/test-reporter-latest-linux-amd64 > ./cc-test-reporter
  - chmod +x ./cc-test-reporter
  - ./cc-test-reporter before-build
script:
  - adb logcat *:I & LOGCAT_PID=$!
  - ./gradlew build connectedCheck jacocoTestReport
after_script:
  # Report test coverage to Code Climate
  - export JACOCO_SOURCE_PATH=app/src/main/java/
  - ./cc-test-reporter format-coverage ./app/build/reports/jacoco/jacocoTestReport/jacocoTestReport.xml --input-type jacoco
  - ./cc-test-reporter upload-coverage