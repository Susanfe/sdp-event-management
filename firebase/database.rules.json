{
  "rules": {
    ".read": true,
    ".write": false,
    "events": {
      "$event_id": {
        ".read": true,
        ".write": "auth != null && (data.child('users').child('admin').val() === auth.uid || (!data.child('users').exists() && !data.child('name').exists()))"
      },
    },
    "ratings": {
      ".read": true,
      ".write": true
    }
  }
}